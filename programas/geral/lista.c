#include <stdio.h>


typedef struct Node {
    int valor;
    struct Node* prox;
} Node;

Node* inicializaLista() {
    return NULL;
}

Node* insert(Node* Lista, int valor) {
    Node* aux = 
}

int main(int argc, char *argv[]) {
    Node* lista = inicializaLista();

    return 0;
}
